extends layout

block content
    div.images-show
        h2 Images
        div.row
            each img in imgs
                //- - let str = `data:image/png;base64, ${image}`
                div.col-sm-8.col-md-4.col-lg-
                    img.image(src=img.src )
